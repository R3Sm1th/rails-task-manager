<h1> List of Tasks </h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %> | <%= task.details %> | <%= task.completed %> - <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<br>
<%= link_to 'New Task', tasks_new_path() %>
